# 使用轻量级的 Python 镜像
FROM python:3.9-slim

# 设置工作目录
WORKDIR /app

# 安装必要的依赖 (Flask 和 Werkzeug)
RUN pip install --no-cache-dir flask werkzeug

# 将当前目录下的代码拷贝到镜像中
COPY . .

# 创建上传目录
RUN mkdir -p uploads

# 暴露端口
EXPOSE 5000

# 启动程序
CMD ["python", "app.py"]